server:
  port: 9001
spring:
  application:
    name: codesocial-base #指定服务名
  rabbitmq:
    host: 192.168.80.129
  sms:
    ip: 127.0.0.1
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/codesocial_base?characterEncoding=utf-8
    username: root
    password: root
  jpa:
    database: MySQL
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6868/eureka
  instance:
    prefer-ip-address: true
 
